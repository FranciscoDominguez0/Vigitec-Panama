services:
  web:
    build: .
    ports:
      - "8087:80"
    volumes:
      - .:/var/www/html
    env_file:
      - .env
    depends_on:
      - mailhog

  mailhog:
    image: mailhog/mailhog
    ports:
      - "1025:1025" # SMTP port
      - "8025:8025" # Web UI port
